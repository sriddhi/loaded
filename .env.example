POSTGRES_PASSWORD=loaded_dev
NEXT_PUBLIC_API_URL=http://localhost:9000

# Host ports (loaded uses 9000-9999 for the API and 4000-4999 for the frontend).
BACKEND_PORT=9000
FRONTEND_PORT=4000

# CORS — comma-separated list of allowed origins.
# Leave empty to allow all origins (dev only — lock this down in production).
# Example: ALLOWED_ORIGINS=https://app.loaded.com,https://admin.loaded.com
ALLOWED_ORIGINS=

# Alpaca Paper account (https://app.alpaca.markets/paper/dashboard/overview)
ALPACA_PAPER_API_KEY=
ALPACA_PAPER_SECRET_KEY=

# Alpaca Real Money account (https://app.alpaca.markets/live/dashboard/overview)
# Leave empty to disable real money trading
ALPACA_REAL_API_KEY=
ALPACA_REAL_SECRET_KEY=

# Anthropic (https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=

# Finnhub (https://finnhub.io/dashboard) — free tier: US trades, <=50 symbols.
# Used for the live-price websocket + earnings calendar in the fundamentals module.
FINNHUB_API_KEY=

# Fundamentals refresh layer. Lazy-TTL freshness on reads + earnings-aware ingest.
FUNDAMENTALS_TTL_TRACKED_DAYS=30
FUNDAMENTALS_TTL_ADHOC_DAYS=7
EARNINGS_POLL_MINUTES=30
EARNINGS_AGEOUT_TRADING_DAYS=2
EARNINGS_POLL_WINDOW=06:00-22:00

# JWT Auth (generate with: openssl rand -hex 32)
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Default admin user (seeded on first startup if both are set)
ADMIN_EMAIL=admin@loaded.app
ADMIN_PASSWORD=

# Google OAuth (https://console.cloud.google.com/apis/credentials)
# Authorized redirect URI must match GOOGLE_REDIRECT_URI exactly.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/api/auth/google/callback
FRONTEND_URL=http://localhost:4000

# Auth cookies. Set COOKIE_SECURE=true in production (HTTPS).
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# SPY signal job cadence (seconds)
SIGNAL_INTERVAL_SECONDS=60

# ── Strategy Lab chat provider ──────────────────────────────────────────────
# 'api'        → uses ANTHROPIC_API_KEY (needs API credits)
# 'claude_code'→ uses your local Claude Code subscription via scripts/claude_bridge.py
#                Run the bridge on the HOST:  python3 scripts/claude_bridge.py
STRATEGY_CHAT_PROVIDER=api
CLAUDE_BRIDGE_URL=http://host.docker.internal:8787
CLAUDE_BRIDGE_TOKEN=
