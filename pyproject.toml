[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
ignore = ["E501", "B008"]  # B008: Depends() in FastAPI signatures is the framework pattern

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.11"
# Mandatory type safety: every function's input params and return type must be
# annotated; no implicit Any, no bare generics, no untyped calls.
strict = true
ignore_missing_imports = true
warn_unused_ignores = true
explicit_package_bases = true
exclude = ["backend/tests/"]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
asyncio_mode = "auto"
